listener {
    timeout = 350 # 5min
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 1800 # 30min
#    on-timeout = systemctl suspend #very broken for unknown reasons
}
